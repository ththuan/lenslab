services:
  lenslab-ai:
    build: .
    ports:
      - "8000:8000"
    environment:
      LENS_AI_ALLOWED_ORIGINS: "*"
      LENS_AI_MODEL_DIR: /app/models
    volumes:
      - lenslab-models:/app/models
    restart: unless-stopped

volumes:
  lenslab-models:

